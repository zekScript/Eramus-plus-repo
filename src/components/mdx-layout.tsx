export default function Introduction( { params } : {
      params: {slug: string}
} ){
      
      if(params.slug == "dx"){

      
      }


      return(

            <>
            Hello world from NOT MDX
            </>
      )
      
}